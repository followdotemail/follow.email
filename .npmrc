# Enable workspaces
enable-pre-post-scripts=true

# Hoist dependencies to root node_modules
hoist=true

# Use exact versions
save-exact=false

# Fail fast on peer dependency conflicts
strict-peer-dependencies=false

# Legacy peer deps for compatibility
legacy-peer-deps=false

